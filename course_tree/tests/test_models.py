"""
TODO test FileNode, especially the mime inference & thumbnail generation processes,
especially in edge cases to show that it's robust and doesn't block the service
if something goes wrong
"""
from django.test import TestCase


class BaseTestCase(TestCase):
    pass


class NodeModelsTestCase(BaseTestCase):
    pass
